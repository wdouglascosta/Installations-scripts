git clone https://github.com/wmfsystem/Project_financeiro.git

git remote add upstream https://github.com/wmfsystem/Project_financeiro.git

git remote -v

touch teste_seunome
git add .
git commit -m "seu comentario"

git pull --rebase

git push -u origin master
